/**
 * Data Transfer Objects.
 * <p>
 * This package contains all the DTOs used in the application.
 * Ce package contient tous les DTO utilisés dans l'application.
 * <br>
 * A DTO is an object that carries data between processes.
 * <br>
 * Un DTO est un objet qui transporte des données entre les processus.
 * <br>
 * A DTO does not have any behavior except for storage and retrieval of its own data.
 * <br>
 * Un DTO n'a aucun comportement sauf le stockage et la récupération de ses propres données.
 * <br>
 * In DTOs, data is transferred as simple data structures, which do not contain any business logic.
 * <br>
 * Dans les DTO, les données sont transférées sous forme de structures de données simples, qui ne contiennent aucune logique métier.
 * <br>
 * The primary purpose of using a DTO is to encapsulate data and send it over the network.
 * <br>
 * Le but principal d'utiliser un DTO est d'encapsuler les données et de les envoyer sur le réseau.
 * <br>
 * DTOs are mainly used to transfer data between the client and the server.
 * <br>
 * <p>
 * Les DTO sont principalement utilisés pour transférer des données entre le client et le serveur.
 * <br>
 * DTOs are also used to transfer data between different layers of an application.
 *     <div>
 *         <img src="https://static.javatpoint.com/tutorial/dto/images/dto-in-java.png" alt="DTO in Java">
 *      </div>
 *      <br>
 * </p>
 */

package com.tonde.maisonchapback.services.dto;